(ns lomake-editori.application.view
  (:require [re-frame.core :as re-frame]
            [reagent.core :as r]
            [re-com.core :as re-com]))

(defn application []
  (fn []
    [:div "application"]))
