/*
  Client configuration is passed as a script tag object in a rendered template.
  It's path has to be externed so that Google Closure  compiler doesn't obfuscate the
  reference
*/

var config = function(){};
config.applicant = function(){};
config.applicant.service_url = function(){};
